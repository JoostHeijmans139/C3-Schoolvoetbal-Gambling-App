<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="GamblingApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GamblingApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="GamblingApp">

    <Grid x:Name="rootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="9*"/>
        </Grid.RowDefinitions>
        <SelectorBar x:Name="navigationSelectorBar" SelectionChanged="navigationSelectorBar_SelectionChanged">
            <SelectorBarItem Text="Home" IsSelected="True"/>
            <SelectorBarItem Text="Toekomstige wedstrijden"/>
        </SelectorBar>
        <ProgressRing Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" x:Name="loadingProgressRing" Width="100" Height="100" IsActive="True"/>
        <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
            <Button x:Name="refreshButton" Click="refreshButton_Click">
                <FontIcon Glyph="&#xE72C;"/>
            </Button>
            <Button x:Name="pointsButton" Margin="0,0,10,0" Click="pointsButton_Click">
                <StackPanel Orientation="Horizontal">
                    <InfoBadge FontSize="15" VerticalAlignment="Center" Value="{Binding Points, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock FontSize="15" Margin="5,0,0,0" VerticalAlignment="Center">Punten</TextBlock>
                </StackPanel>
            </Button>
        </StackPanel>
        <Frame Grid.Row="1" x:Name="contentFrame" Opacity="0.4" IsHitTestVisible="False"/>
        <InfoBar Grid.Row="1" x:Name="messageInfoBar" IsOpen="False"></InfoBar>
    </Grid>
</Window>
