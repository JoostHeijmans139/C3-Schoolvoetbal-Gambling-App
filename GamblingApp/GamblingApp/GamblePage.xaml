<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="GamblingApp.GamblePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GamblingApp"
    xmlns:data="using:GamblingApp.Data"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ListView x:Name="gameListView" IsItemClickEnabled="False" ItemsSource="{Binding FutureGames, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <ListView.Header>
                <TextBlock Margin="8,10,8,10" FontSize="25">Toekomstige wedstrijden:</TextBlock>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="data:Game">
                    <Border BorderBrush="{ThemeResource DividerStrokeColorDefault}" BorderThickness="0,0,0,1" Padding="8">
                        <Grid Margin="0,10,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="3*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="2*"/>
                                <RowDefinition Height="10px"/>
                                <RowDefinition Height="4*"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Grid.ColumnSpan="3">
                                <TextBlock FontWeight="Bold" FontSize="30" Text="{Binding Tournament.Name}"/>
                                <TextBlock Text="{Binding FormattedStart}"/>
                            </StackPanel>

                            <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,0,0">
                                <TextBlock HorizontalAlignment="Center" FontSize="30" Text="{Binding Team1.Name}" />
                            </StackPanel>

                            <TextBlock Grid.Row="2" Grid.Column="1" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center">VS</TextBlock>

                            <StackPanel Grid.Row="2" Grid.Column="2">
                                <TextBlock HorizontalAlignment="Center" FontSize="30" Text="{Binding Team2.Name}" />
                                <Button x:Name="betButton" Click="betButton_Click" FontSize="20" Padding="20,10,20,10" HorizontalAlignment="Center" Margin="0,30,0,0">
                                    Inzetten
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <InfoBar x:Name="errorInfoBar">
            
        </InfoBar>
    </Grid>
</Page>
