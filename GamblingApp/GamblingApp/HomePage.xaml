<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="GamblingApp.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GamblingApp"
    xmlns:data="using:GamblingApp.Data"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ProgressRing x:Name="loadingProgressRing" Width="100" Height="100" IsActive="True"/>
        <InfoBar x:Name="messageInfoBar" IsOpen="False"></InfoBar>
        
        <ListView x:Name="gameListView">
            <ListView.Header>
                <TextBlock Margin="8,10,8,10" FontSize="25">Recente wedstrijden:</TextBlock>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="data:Game">
                    <Grid Margin="0,10,0,10" Padding="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="3*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="3*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="2*"/>
                            <RowDefinition Height="10px"/>
                            <RowDefinition Height="4*"/>
                        </Grid.RowDefinitions>
                        
                        <StackPanel Grid.Row="0" Grid.ColumnSpan="67">
                            <TextBlock FontWeight="Bold" FontSize="30" Text="{Binding Tournament.Name}"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,0,0">
                            <TextBlock HorizontalAlignment="Center" FontSize="30" Text="{Binding Team1.Name}" />
                            <TextBlock FontSize="20" HorizontalAlignment="Center" Text="{Binding ScoreTeam1}"/>
                        </StackPanel>

                        <TextBlock Grid.Row="2" Grid.Column="1" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center">VS</TextBlock>

                        <StackPanel Grid.Row="2" Grid.Column="2">
                            <TextBlock HorizontalAlignment="Center" FontSize="30" Text="{Binding Team2.Name}" />
                            <TextBlock FontSize="20" HorizontalAlignment="Center" Text="{Binding ScoreTeam1}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
