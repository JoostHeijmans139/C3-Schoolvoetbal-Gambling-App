<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="GamblingApp.BetHistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GamblingApp"
    xmlns:data="using:GamblingApp.Data"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <ScrollView>
        <StackPanel>
            <TextBlock Margin="8,10,8,10" FontSize="25">Bet History:</TextBlock>
            <GridView x:Name="historyListView" CanDragItems="True">
                <GridView.ItemContainerStyle>
                    <Style TargetType="GridViewItem" BasedOn="{StaticResource DefaultGridViewItemStyle}">
                        <Setter Property="Margin" Value="8"/>
                        <Setter Property="Padding" Value="15"/>
                        <Setter Property="Background" Value="#303030"/>
                    </Style>
                </GridView.ItemContainerStyle>
                <GridView.ItemTemplate>
                    <DataTemplate x:DataType="data:Bet">
                        <Grid Height="150" Width="500">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="3*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="3*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Spacing="5">
                                <TextBlock Text="Ingezet:"/>
                                <TextBlock Text="{Binding Amount}"/>
                            </StackPanel>
                            <TextBlock Grid.Row="0" Grid.Column="3" Grid.ColumnSpan="2" HorizontalAlignment="Right" Text="{Binding PlacedAt}"/>


                            <TextBlock Text="{Binding Game.Team1.Name}" Grid.Column="1" Grid.Row="2" HorizontalAlignment="Center"/>
                            <TextBlock Grid.Column="2" Grid.Row="2" HorizontalAlignment="Center">VS</TextBlock>
                            <TextBlock Text="{Binding Game.Team2.Name}" Grid.Column="3" Grid.Row="2" HorizontalAlignment="Center"/>

                            <TextBlock Grid.Column="1" Grid.Row="3" HorizontalAlignment="Center" Text="{Binding Game.ScoreTeam1}"/>
                            <TextBlock Grid.Column="3" Grid.Row="3" HorizontalAlignment="Center" Text="{Binding Game.ScoreTeam2}"/>


                            <TextBlock Grid.Column="1" Grid.Row="4" Grid.ColumnSpan="3" Grid.RowSpan="2" FontSize="20" HorizontalAlignment="Center" Text="{Binding Status}"/>
                        </Grid>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
        </StackPanel>
    </ScrollView>
</Page>
